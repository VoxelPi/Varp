package net.voxelpi.varp

object VarpBuildParameters {

    /**
     * The current version.
     */
    const val VERSION: String = "{{ version }}"

    /**
     * The current git commit.
     */
    const val GIT_COMMIT: String = "{{ git_commit }}"

    /**
     * The current git commit.
     */
    const val GIT_BRANCH: String = "{{ git_branch }}"
}
